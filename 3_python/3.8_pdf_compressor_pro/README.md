PDF Compressor Pro
Профессиональное приложение для сжатия PDF файлов с графическим интерфейсом и базой данных.

🚀 Возможности
Интеллектуальное сжатие PDF файлов с использованием Ghostscript

Графический интерфейс на Tkinter для удобной работы

База данных SQLite для отслеживания обработанных файлов

Предотвращение повторной обработки - файлы обрабатываются только один раз

Гибкие настройки сжатия с возможностью сохранения пресетов

Подробное логирование операций и статистики

Поддержка сетевых путей и работа с файлами на удаленных ресурсах

📋 Требования
Python 3.8+

Ghostscript (автоматическая проверка при запуске)

Зависимости из requirements.txt

🛠 Установка
Установите Ghostscript:

Скачайте с официального сайта

Или через пакетный менеджер:

bash
# Windows (chocolatey)
choco install ghostscript

# Linux (apt)
sudo apt-get install ghostscript

# macOS (brew)
brew install ghostscript
Установите зависимости Python:

bash
pip install -r requirements.txt
Запустите приложение:

bash
# Через Python
python main.py

# Или через bat-файл (требует прав администратора)
Start_PDF_Compressor.bat
🗃 Структура базы данных
Основные таблицы:
processed_files - история обработки файлов

setting - настройки сжатия (пресеты)

fail_reason - причины ошибок обработки

nesting_depth - уровни вложенности директорий

compression_method - методы сжатия

Ключевые особенности БД:
Уникальные constraint для предотвращения дублирования настроек

Связи между таблицами через внешние ключи

Автоматическое логирование всех операций

История обработки с детальной статистикой

⚙️ Настройки сжатия
Уровни сжатия:
Экономный - максимальное сжатие, подходит для веб

Стандартный - баланс качества и размера

Максимальный - лучшее качество, умеренное сжатие

Дополнительные параметры:
Глубина вложенности - обработка поддиректорий

Замена исходных файлов - авто-замена или создание копий

Минимальный порог сжатия - пропуск файлов с малым выигрышем

Таймаут обработки - защита от зависаний

📊 Статистика и логирование
Реальное время обработки файлов

Детальная статистика по экономии места

Визуализация прогресса сжатия

Автоматическое ротирование лог-файлов

Экспорт отчетов в текстовом формате

🎯 Использование
Выберите директорию для обработки

Настройте параметры сжатия или используйте существующий пресет

Запустите обработку

Отслеживайте прогресс в реальном времени

Просматривайте детальную статистику после завершения

🔧 Технические особенности
Многопоточная обработка с защитой от блокировок UI

Поддержка Unicode путей и сетевых ресурсов

Автоматическое восстановление после ошибок

Временные файлы с уникальными именами

Проверка целостности перед заменой файлов

📝 Примечания
Для работы с сетевыми файлами требуются права администратора

Рекомендуется закрыть обрабатываемые PDF файлы перед запуском

Программа автоматически пропускает уже обработанные файлы

Логи хранятся в папке logs/ с автоматическим ротированием

🐛 Решение проблем
Если возникают ошибки:

Убедитесь, что Ghostscript установлен и доступен в PATH

Проверьте права доступа к целевым директориям

Закройте открытые PDF файлы в других программах

Увеличьте таймаут обработки для больших файлов

Разработано для эффективного управления PDF архивами 📄✨